#include<stdio.h>

int main() {
    char a[4] = "213";  
// Increased array size to accommodate null terminator
    int res = 0;

    for(int i=0; i<3; i++)
    {
        res = (res * 10) + (a[i] - '0');  // Fixed subtraction syntax
    }

    printf("%d", res);

    return 0;
}
